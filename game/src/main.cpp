#include <iostream>

#include "application.h"
#include "engine.h"

// #include <SDL3/SDL.h>

// #include <SDL3/SDL.h>

int main(int argc, char *argv[])
{
    engine eng;
    // SDL_Init(SDL_INIT_EVERYTHING);
    std::cout << "Starting game."
              << "\n";
    application app;

    return 0;
}